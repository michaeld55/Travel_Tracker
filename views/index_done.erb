<title>My Completed Trips</title>
<% counter = 0  %>
<% @trips.each do |trip|  %>
   <p>
      <% @cities_done = Trip.find_done( trip.id ) %>
      <% if @cities_done.size > 0 %>
        <% counter += 1  %>
        <%= "Trip #{trip.id}:"%>
        <% @country_destinations = trip.find_country_cities %>
        <% @country = @country_destinations[0] %>
        <% @country_destinations.shift %>
        <% @destinations = @country_destinations  %>
        <%= " You have Completed the trip to #{@country.name} and visited:" %>
        <% destination_counter = @cities_done.size  %>
        <% @cities_done.each do |city|%>
        <% if destination_counter > 1  %>
            <%= "#{city.name}, " %>
            <% destination_counter -= 1  %>
          <% elsif destination_counter == 1 %>
            <%= "#{city.name}." %>
            <% destination_counter -= 1  %>
          <% end  %>
        <% end  %>
        <form action= "/trips/<%= "#{trip.id}"%>/edit" method="get">
        <input type="submit" value="Edit Trip">
        </form>
        <form method="get" action="/trips/<%="#{trip.id}"%>/delete">
        <input type="submit" value="Delete Trip">
        </form>
      </p>
    <br>
  <% end %>
<% end  %>
